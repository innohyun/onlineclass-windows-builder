{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the local sensitive store status window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
